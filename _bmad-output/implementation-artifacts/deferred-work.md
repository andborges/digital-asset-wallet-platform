# Deferred Work

Items deferred during reviews — real but not actionable at the time they were found.

## Deferred from: code review of 1-2-query-customer-balances (2026-07-14)

- **Raw internal error text leaked in problem+json `detail` on 500** [internal/adapter/api/customers.go:66] — `WriteProblem(..., err.Error(), ...)` forwards wrapped DB error text (query structure, schema names, SQL state) to clients. Pre-existing project-wide pattern (`CreateCustomer`'s 500 branch does the same); `problem.go` already warns against sensitive `Detail`. Fix globally: log the detail server-side and return a generic client-facing message.
- **No object-level authorization on per-customer reads** [internal/adapter/api/customers.go:59] — the auth model is a static shared-token allowlist with no notion of caller identity or customer ownership, so any valid token can read any customer's balances by id. This is the first read endpoint to expose per-customer financial state through that model. Likely by-design for the B2B "application team" caller, but the trust model should be explicitly confirmed before any end-user-facing exposure.
- **Existence check + balance SUM run as two separate pool queries outside a transaction (TOCTOU)** [internal/adapter/postgres/balance_repo.go:36-47] — benign today (read-only, no delete path, no posting writer), but once Story 1.3 adds concurrent writes, consider folding existence into a single round-trip (CTE / RIGHT JOIN) for a consistent snapshot.
- **Negative derived balance surfaced with no guard** [internal/adapter/postgres/balance_repo.go:48] — `postings.amount` is signed `NUMERIC(78,0)`, so `SUM` can go negative and the read path emits it verbatim. No writer exists until Story 1.3; decide on a non-negative floor (read-path guard and/or DB CHECK) before rows are written.

## Deferred from: code review of 1-3-ledger-only-internal-transfer-between-customers (2026-07-15)

- **Raw internal error text leaked in problem+json `detail`** [internal/adapter/api/transfers.go:55-70] — recurs on the transfers endpoint: 500 branches forward wrapped SQL context and the 404 echoes the caller's customer UUID. Same platform-wide item first logged in the 1-2 review; fix globally (log server-side, return a generic client message), not per-endpoint.
- **No upper bound on transfer amount vs NUMERIC(78,0)** [internal/adapter/postgres/transfer_repo.go:118] — a >78-digit amount would surface as a 500 (numeric overflow) rather than a clean 400. Practically unreachable today (the balance check returns 422 first), but a boundary validation would make the money boundary explicit.
- **Concurrency (deadlock) regression test for the two-account lock** [internal/adapter/postgres/transfer_repo.go:34-45] — deferred to Story 6.3 per the 1-3 spec: the `SELECT … ORDER BY id FOR UPDATE` deadlock-avoidance pattern is assessed correct but proven by no test. Reason: 6.3's consolidated fault-injection suite is where NFR1 concurrency claims are exercised systematically rather than piecemeal per story. A live A→B-racing-B→A regression test should land there.
- **journal_entries unique-violation not disambiguated by constraint name** [internal/adapter/postgres/transfer_repo.go:99-104] — any `23505` on the journal-entry insert is mapped to `ErrDuplicateTransferCause` (409). Only the `cause_id` constraint is realistic now (the PK is a fresh UUIDv7); check `pgErr.ConstraintName` before returning 409 so a future added constraint isn't misreported.

## Deferred from: code review of 1-4-list-customer-transaction-history (2026-07-15)

- **Raw internal error text leaked in problem+json `detail` on 500** [internal/adapter/api/transactions.go:40] — recurs on the transactions endpoint: `WriteProblem(..., err.Error(), ...)` forwards wrapped DB/SQL error text to clients. Same platform-wide item first logged in the 1-2 review and again in 1-3; fix globally (log server-side, return a generic client-facing message), not per-endpoint. *(1-5 re-review, 2026-07-16: recurs again in the new `GetCustomer` handler [internal/adapter/api/customers.go:70,79].)*
- **No composite index backing the pagination sort** [internal/adapter/postgres/transaction_repo.go:110] — the query orders by `je.created_at DESC, je.id DESC` with no index on `journal_entries(created_at, id)` (only `idx_postings_account_id` exists), so each page sorts the customer's entire matched history; cost grows with total history size, not page size — the anti-pattern keyset pagination is meant to avoid. Story 1.4 deliberately adds no migration; add a composite index when a migration is next opened for this schema.

## Deferred from: code review of 1-5-generate-per-customer-deposit-address-via-create2-forwarder (2026-07-15)

- **`GetCustomer`'s INNER JOIN reports an existing customer as 404 if it lacks a `deposit_addresses` row; the 1:1 invariant is not FK-enforced** [internal/adapter/postgres/customer_reader.go:34-42] — unreachable in greenfield (current `CreateCustomer` writes customer + address atomically), so not actionable now. Becomes relevant if any pre-migration or partial-write customer row ever exists; consider enforcing the 1:1 relationship structurally (or a defensive existence check) before real data accumulates.
- **Deployer-presence check accepts any contract code at the deployer address, not the canonical deployer's specific bytecode** [internal/adapter/evm/deployer.go:42] — AC3 says "presence," which `len(code) != 0` satisfies, so this is hardening beyond the AC. On a forked/misconfigured chain where `0x4e59…` holds a *different* contract, the check passes and derived addresses could diverge on-chain. A `keccak256(code)` comparison against the known deployer runtime-code hash would make the guarantee real.

## ⚠️ BLOCKING BEFORE PRODUCTION — from Story 1.5 (2026-07-16)

- **`Forwarder.sol`'s `TREASURY` constant is a placeholder (`0x…dEaD`, the burn address) and MUST be replaced with the real hot-wallet address at the Story 6.2 key ceremony, BEFORE the first production deposit address is issued** [contracts/src/Forwarder.sol:45] — `flush()`/`flushToken()` are permissionless and can only send to `TREASURY`; with the placeholder, every sweep permanently burns customer funds. Replacing it changes the Forwarder bytecode and therefore re-derives every deposit address (regeneration procedure: contracts/README.md), which is acceptable only while no address has been issued to a real customer. This was an explicit, user-confirmed decision in the 1-5 rework (trading the earlier owner-locked-funds bug for a scheduled pre-production bytecode break); no mechanical gate can distinguish "production" at build time, so this tracker entry + the loud comments in Forwarder.sol and contracts/README.md are the guard. Story 6.2 must treat this as an acceptance criterion.

## Deferred from: code review of 1-5-generate-per-customer-deposit-address-via-create2-forwarder — re-review (2026-07-16)

- **Every restart requires both third-party RPC probes to succeed before the API serves** [cmd/walletd/main.go:94-107] — the AC3 startup gate (correct to spec: "fails startup loudly") has no retry and no memory of prior success, so a transient Base or Arbitrum provider outage during a crash-restart or deploy blocks the entire customer API — whose serving paths are pure Postgres and need no chain. Revisit the availability tradeoff (retry with backoff, or verify-once-and-record) as an ops concern before production (Epic 5/6 territory).
- **Idempotency replays of pre-change stored responses can violate a since-tightened response schema** [internal/adapter/api/idempotency.go; api/openapi.yaml] — `idempotency_records` has no TTL, so a `CreateCustomer` 201 body stored before Story 1.5 replays without the now-required `depositAddress`. Unreachable in fresh environments; only matters for environments deployed pre-change. First instance of a general policy question: how stored idempotent responses interact with response-shape evolution (flush on deploy, version records, or accept).
- **`integration_test.go` imports `internal/adapter/evm` (adapter→adapter, test-only)** [internal/adapter/api/integration_test.go] — endorsed in the 1-5 rework as a composition-root-like usage (`evm.IsChecksummedAddress`), but Story 2.1's import-boundary CI check must explicitly define its `_test.go`/composition-root policy or it will trip on this import.

## Deferred from: code review of spec-2-1-track-incoming-deposits-and-expose-pending-status (2026-07-16)

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-track-incoming-deposits-and-expose-pending-status.md`
  summary: Internal ETH transfers (a contract `CALL`/`SELFDESTRUCT` sending value to a deposit address) are invisible to the watcher's top-level-transaction scanning.
  evidence: `internal/adapter/evm/scanner.go`'s `scanNativeTransfers` only inspects `block.Transactions()`'s top-level `tx.To()`/`tx.Value()` — a real, currently-reachable way to fund a deposit address that the scanner will never observe. Full coverage needs a trace API (`debug_traceBlock` or similar), which is costly and often restricted on public RPC providers; this was never in Story 2.1's committed design (Design Notes specify top-level scanning only).
- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-track-incoming-deposits-and-expose-pending-status.md`
  summary: A nonzero-value transaction to a *deployed* Forwarder that reverts (e.g., underfunded gas) would be recorded as an observed deposit with no receipt-status check.
  evidence: `internal/adapter/evm/scanner.go`'s `scanNativeTransfers` never fetches a transaction receipt. Verified unreachable today: deposit addresses are counterfactual/code-less until Story 3.6 deploys the Forwarder, and a plain value transfer to a no-code address cannot revert post-inclusion; `Forwarder.sol`'s `receive()` is a permissive no-op even after deployment (`contracts/src/Forwarder.sol`). Revisit only if `receive()`/`fallback` ever gains revert-capable logic.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-track-incoming-deposits-and-expose-pending-status.md`
  summary: Re-running `oapi-codegen` after adding the `Deposit` schema renamed some pre-existing enum constants (e.g. `TransferRequestAssetEth` → `Eth`) because several schemas inline the same `chain`/`asset` enum values instead of `$ref`-ing shared component schemas.
  evidence: `internal/adapter/api/server.gen.go` — a pre-existing `api/openapi.yaml` duplication pattern (since Story 1.3/1.4, each of `TransferRequest`/`Balance`/`Transaction` inlines its own `chain`/`asset` enum rather than referencing shared `Chain`/`Asset` component schemas), not introduced by this story; adding the `Deposit` schema's overlapping enum values just tipped the generator's naming-collision avoidance into renaming some constants. `go build`/`go vet` both pass — nothing is broken, only inconsistently named. Worth a global fix (extract shared component schemas) outside this story's scope.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-track-incoming-deposits-and-expose-pending-status.md`
  summary: `NewServerInterface`'s constructor keeps growing as unlabeled positional `*core.X` arguments (6 as of this story).
  evidence: `internal/adapter/api/customers.go` — a pre-existing pattern extended incrementally by every story adding a use case (1.1 through 2.1), not this story's alone to redesign; the compiler catches reordering mistakes since every parameter type differs, but readability debt keeps accruing.

## Deferred from: code review of spec-2-2-credit-deposits-at-finalized-tier-via-policy-table (2026-07-17)

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-credit-deposits-at-finalized-tier-via-policy-table.md`
  summary: `CreditFinalizedDeposits` has no per-row failure isolation — one hard DB error mid-batch aborts the whole poll's credit phase (and, via the shared transaction, that poll's scan/promotion work too).
  evidence: `internal/adapter/postgres/deposit_repo.go`'s credit loop runs every eligible deposit inside the single transaction `core.TrackDeposits.Execute` opens for the whole poll cycle; a failure on deposit N rolls back deposits 1..N-1's writes too, and the same offending row is re-selected on every subsequent poll with nothing to skip or dead-letter it. The most likely realistic trigger (a non-positive amount) is closed by this review's amount-validation patch; genuine per-row isolation (savepoints, or moving away from one-transaction-per-poll) is a bigger architectural change, appropriate for Epic 6's consolidated fault-injection work rather than a quick patch here.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-credit-deposits-at-finalized-tier-via-policy-table.md`
  summary: A missing `accounts`/`crediting_policy` row for a `(chain, asset)` pair silently excludes any matching deposit from ever being credited, with the deposit becoming invisible on both the pending-deposits and transaction-history endpoints simultaneously.
  evidence: `internal/adapter/postgres/deposit_repo.go`'s `CreditFinalizedDeposits` INNER JOINs to `accounts` (customer + platform float) and `crediting_policy`, all silently excluding a deposit from the result set if any join misses — no error, no log, no metric. Unreachable in greenfield: `CreateCustomer` always creates all 4 per-customer accounts atomically, and migration 0006 seeds all 4 platform float accounts + all 4 policy rows for the current fixed `core.SupportedChainAssetPairs`. Becomes relevant only if a future chain/asset is added to that set without corresponding seed rows in a follow-up migration.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-credit-deposits-at-finalized-tier-via-policy-table.md`
  summary: No fault-injection test forces a mid-batch failure in `CreditFinalizedDeposits` to verify multi-deposit atomicity.
  evidence: The code's own comment in `internal/core/track_deposits.go` claims a partial failure during the finalize/credit phase leaves every deposit row, cursor, and ledger entry exactly as before, but nothing actually injects a failure on, say, the second of several eligible deposits to prove the rollback is clean. Deferred to Story 6.3's consolidated fault-injection suite, the same precedent already set for Story 1.3's lock-ordering deadlock regression test.

## Deferred from: code review of spec-2-3-handle-unsupported-token-transfers-without-ledger-corruption (2026-07-17)

- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-handle-unsupported-token-transfers-without-ledger-corruption.md`
  summary: The new platform-wide `GET /unsupported-token-observations` endpoint widens the blast radius of this system's shared-bearer-token auth model — any valid token can now enumerate every customer's deposit address in a single call, not just look one up at a time.
  evidence: `internal/adapter/api/unsupported_token_observations.go` requires only the same bearer auth as every other route; there is no per-tenant/operator role distinction anywhere in this system. Same root limitation first logged in the 1-2 review ("No object-level authorization on per-customer reads... likely by-design for the B2B 'application team' caller") and repeatedly deferred since, but this endpoint's aggregation (platform-wide list vs. one-customer-at-a-time lookup) is a meaningfully larger escalation worth tracking distinctly. Fixing the auth model is a cross-cutting change well beyond any single story's scope.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-handle-unsupported-token-transfers-without-ledger-corruption.md`
  summary: The two real-anvil scanner tests duplicate nearly all of their anvil-startup/contract-deployment boilerplate.
  evidence: `internal/adapter/evm/scanner_test.go`'s `TestScanner_RealAnvil_FindsNativeAndUSDCTransfers` and `TestScanner_RealAnvil_ClassifiesUnregisteredTokenAsUnsupported` differ only in the token registry map passed to `ScanDeposits` and the final assertions; a shared helper would cut CI overhead and stop future plumbing changes (gas parameters, bytecode) from needing to be applied twice by hand. Maintainability nice-to-have, not a correctness issue.

## Deferred from: code review of spec-2-4-detect-and-safely-reverse-reorged-pre-finality-deposits (2026-07-17)

- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-detect-and-safely-reverse-reorged-pre-finality-deposits.md`
  summary: `Scanner.BlockHash`'s not-found/mismatch signal is a single RPC call per poll with no retry — a transient provider hiccup could in principle be misread as "the chain got shorter," permanently and irreversibly orphaning a legitimate deposit with no recovery path.
  evidence: `internal/adapter/evm/scanner.go`'s `BlockHash` returns `exists=false` on the first `ethereum.NotFound` response and `checkForReorgs` (`internal/core/track_deposits.go`) orphans on that signal with no re-check. Not patched: the current architecture assumes one dedicated RPC endpoint per chain (no load-balanced/multi-replica setup exists anywhere in this codebase's config model — that concern is AD-12/Epic 5's territory, reconciliation-only), under which a spurious not-found/mismatch essentially requires an actual reorg. Hardening against an unreliable or multi-replica RPC setup is a larger undertaking for Epic 6's fault-injection work or whenever the RPC-provider model is generalized.

## Deferred from: code review of spec-2-5-recover-from-watcher-downtime-without-loss-or-double-processing (2026-07-17)

- source_spec: `_bmad-output/implementation-artifacts/spec-2-5-recover-from-watcher-downtime-without-loss-or-double-processing.md`
  summary: The mid-batch-failure test harness's rollback simulation (`fakeTxBeginner`'s snapshot/restore wiring) only covers `fakeDepositRepo`, not `fakeUnsupportedTokenRepo` — a future test combining an injected failure with non-empty unsupported-token transfers would get a false pass on rollback correctness for that path.
  evidence: `internal/core/track_deposits_test.go`'s `fakeTxBeginner.Begin` snapshots/restores only via its `repo` field; `fakeUnsupportedTokenRepo` is constructed independently in every test with no equivalent mechanism. In the real system, `TrackDeposits.Execute` writes unsupported-token observations inside the same transaction as `RecordObserved` (`track_deposits.go`), so a real mid-batch failure does roll them back too — the test harness just can't currently prove it. Not fixed: this story's mid-batch test is explicitly scoped to the observed-scan/`RecordObserved` loop per its own intent-contract boundary; extending the harness for a path no current test exercises is out of scope here.
