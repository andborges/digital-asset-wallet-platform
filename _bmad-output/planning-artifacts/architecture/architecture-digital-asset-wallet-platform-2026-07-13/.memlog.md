---
scope: Digital Asset Wallet Platform v1 — full system (greenfield Go backend)
purpose: build-substrate
altitude: initiative
updated: 2026-07-14T14:20
---

- (decision) Working mode: Coaching path; deliverables: spine + solution design doc + interactive walkthrough deck (portfolio/consulting audience per PRD vision)
- (constraint) Inherited from PRD (settled, not re-asked): Go backend; Base+Arbitrum; ETH+USDC; credit at finalized; platform-held keys; single-instance best-effort availability with absolute durability; webhook events + platform-owned account/balance model; manual approval above threshold; ledger-only internal transfers; streaming+batch recon; thousands/day + 10x burst; one deposit address per customer reused across EVM chains
- (decision) Paradigm (user): hexagonal ports & adapters governs dependency direction and chain isolation; conventional Go layering (handler->service->repo) organizes code inside the adapters. Prevents chain-specific logic leaking outside adapters (FR33 CI test)
- (decision) Process topology (user): watchers and reconciliation run as separate OS processes, not goroutines in the API process; coordination through Postgres only, no in-process shared state
- (decision) Storage (user): PostgreSQL, single instance, as the sole durable store and the coordination point between processes
- (decision) Ledger model (user): double-entry journal; balances derived from balanced postings, FR5 explainability becomes an arithmetic identity
- (decision) Fund topology (user): CREATE2 forwarder contracts. Deposit addresses are counterfactual minimal-forwarder addresses derived from (factory, salt=customer, init code) — no per-customer key material exists. Funds flow forwarder -> treasury/hot wallet; deploy-on-first-sweep gas model. Consequence: project scope now includes a small audited-pattern Solidity surface (factory + forwarder) alongside the Go service
- (constraint) FR6 cross-chain address equality now depends on: identical factory address, init code, and salt on every supported chain. Factory must be deployed via a deterministic-deployment method (canonical CREATE2 deployer / Nick's method) so a new chain preserves all existing customer addresses
- (decision) Key inventory consequence: the only signing keys in the system are the hot-wallet/treasury key(s) — eligible for cloud KMS custody (secp256k1 sign, key never leaves KMS); no HD master seed, no sweep-signing keys. Mechanism/provider choice pending (coupled to deployment envelope)
- (question) Forwarder contract details deferred to implementation: ETH receive-and-hold vs auto-forward, ERC-20 flush function shape, factory upgradability (lean: none), audit approach for the Solidity surface
- (decision) RPC independence (user): reconciliation process MUST use a different RPC provider than the watchers — independent observation path, or recon-green is meaningless. Managed RPC providers for both; own nodes out of v1 scope
- (decision) Hosting (user): AWS. Hot-wallet key in AWS KMS (secp256k1 ECDSA, key never leaves KMS; software signer behind the same port for dev/test). Prod = single EC2 instance
- (decision) Deployment unit (user): Docker Compose is the deployment/orchestration unit — same compose stack locally and on the AWS VM (api, watcher per chain, recon, webhook dispatcher, Postgres). Fits NFR7 single-instance
- (assumption) Postgres runs inside the compose stack with volume + documented backup/WAL discipline (not RDS) — inferred from Docker-Compose choice; NFR2 durability carried by fsync defaults + backup procedure in ops runbook
- (decision) Exactly-once package (user blessed): (1) one Postgres tx per state change carrying ledger postings + state transition + outbox event atomically; (2) idempotency by unique constraint — API keys table w/ stored response, chain events on (chain,tx_hash,log_index); (3) webhooks via transactional outbox, dispatcher delivers at-least-once w/ backoff, event ID = outbox row ID; (4) nonces by single-writer per (hot wallet x chain) from persisted state; (5) watcher progress as persisted cursors per chain x tier
- (decision) API protocol (user): REST/JSON + OpenAPI contract — first consumer unknown, self-serve-adoption vision makes OpenAPI the documentation, webhooks already JSON/HTTP. gRPC rejected for v1
- (version) Web-verified 2026-07-14: deterministic CREATE2 deployer 0x4e59b44847B379578588920cA78FbF26c0B4956C still the standard — Foundry's default, genesis preinstall on Base (OP Stack), long-deployed on Arbitrum One; RIP-7740 lists it ecosystem-critical. CreateX is an unaudited complement, not needed for v1
- (constraint) Forwarders must be PERSISTENT contracts: EIP-6780 (live on both chains) killed the deploy-flush-selfdestruct-redeploy gas pattern. Reference implementation: BitGo eth-multisig-v4 ForwarderV4 (audited by OpenZeppelin/Quantstamp/CertiK). OZ ERC2771Forwarder is meta-tx, NOT a deposit forwarder
- (version) Web-verified 2026-07-14: AWS KMS ECC_SECG_P256K1 Ethereum signing confirmed (DER->r/s, low-s per EIP-2, recover v by pubkey compare); Go lib moved welthee -> matelang/go-ethereum-aws-kms-tx-signer; cost ~$0.15/10k signs + $1/mo/key, ECC quota 300-1000 req/s — all trivially within v1 volume
- (constraint) EIP-7702 live on Base+Arbitrum: deposit senders may be EOAs with delegated code — no EXTCODESIZE/tx.origin heuristics anywhere in deposit attribution; forwarder address semantics unaffected
- (version) Web-verified 2026-07-14 stack: Go 1.26.x; PostgreSQL 18; go-ethereum v1.17.x (ethclient standard); pgx v5; Foundry v1.7.x (anvil supports anvil_reorg + fork mode — carries NFR19 fault injection); Solidity 0.8.36; AWS SDK Go v2 (v1 EOL); Docker Compose v5; goose v3 migrations; oapi-codegen v2 spec-first with stdlib ServeMux target; testnets Base Sepolia 84532 / Arbitrum Sepolia 421614 current
- (decision) Router: stdlib net/http ServeMux (Go 1.22+ patterns) — now mainstream default, pairs with oapi-codegen std-http target; chi only if middleware needs grow. Migrations: goose (most active, SQL+embed.FS)
- (constraint) Arbitrum Sepolia had a Mar 2026 consensus-failure outage — CI must not depend on live testnet availability; anvil is the deterministic test substrate, testnets are pre-prod only
- (decision) Derived at distill (from single-writer + forwarder decisions): a broadcaster role per chain serializes ALL outbound txs — withdrawals AND forwarder deploy/flush sweeps — through the one hot-wallet key; sweeps are state-machine records like withdrawals. One KMS key = one hot-wallet address valid on both chains, nonce state kept per chain
- (decision) Derived at distill: reconciliation is a read-only observer — it never writes ledger postings; corrections happen only as operator adjustments through the normal audited path. Preserves recon independence structurally
- (change) Reviewer-gate fixes applied (recon: 5 findings; adversarial: 3 critical closed; rubric: 1 high + 4 medium closed; research: pass): watcher = sole deposit writer incl. credit; unique (cause_type,cause_id) on journal entries; forwarder-float account added to taxonomy, sweeps never touch customer balances; broadcaster owns sweep creation w/ one-in-flight unique index; salt derivation pinned (UUID left-padded to bytes32) + persisted addresses + cross-language CI vectors; outbox taxonomy split money/state-only events; recon writes only own tables + alert outbox, owns operational monitoring (watcher lag, liveness, stuck withdrawals, approval-queue age); AD-15 added — degraded chain mode, withdrawals QUEUE never reject; broadcaster advisory-lock enforcement; api fee-read edge added to topology; capacity/latency convention row (NFR4-6); KMS signer lib vendored; BitGo audit confirmation pushed to contracts epic; AD-10 threat-model ratification clause
- (change) Source ripple: PRD addendum unwatched-chain recovery claim corrected — with CREATE2 forwarders funds on unwatched chains are recoverable only on EVM-equivalent chains (same factory deployable), unrecoverable where CREATE2 semantics diverge; logged in PRD memlog too
- (decision) NFR9 fork resolved by architecture (PRD allowed queue OR reject): withdrawals on a degraded chain are accepted and QUEUE; deposits stay pending. Flagged to user in summary
- (event) spine finalized — 15 ADs, gate passed (0 lint findings, all critical/high reviewer findings fixed); renderings delivered: SOLUTION-DESIGN.md (polished) + walkthrough-deck.html (published artifact)
- (decision) UPDATE (user, 2026-07-14): v1 runtime environment is LOCAL — Docker Compose on the developer machine with LocalStack emulating AWS (KMS) and real testnets (Base Sepolia, Arbitrum Sepolia) as the chains. AWS EC2 + real KMS remains the documented production shape for when the platform graduates; anvil + software signer stays the deterministic test substrate. Pending web verification: LocalStack KMS secp256k1 signing support, deterministic deployer presence on both Sepolias
- (version) Web+RPC-verified 2026-07-14: deterministic deployer 0x4e59...956C bytecode confirmed live on Base Sepolia AND Arbitrum Sepolia via direct eth_getCode; LocalStack KMS available in free Hobby plan, supports asymmetric keys (only SM2 spec excluded), ECC_SECG_P256K1 key creation demonstrated in its tracker; residual risk: Sign-with-secp256k1 emulation gap would fall back to the software signer behind the same port — dev-tooling risk only, not architectural
- (change) Spine amended: environments redefined — v1 runtime = local Docker Compose + LocalStack (AWS emulation, KMS via endpoint override on the same adapter) + real Sepolia testnets over two independent RPC providers; test/CI = anvil + software signer (unchanged); AWS EC2 + real KMS retained as documented production shape for graduation. AD-10 amended in place, Stack table + topology label updated. AD IDs stable
- (event) Environment update complete: spine (deployment envelope, AD-10, stack, topology), SOLUTION-DESIGN §8, and deck amended for local-first runtime (LocalStack + Sepolia testnets); lint clean; deck republished same URL
